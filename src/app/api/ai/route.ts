import { NextRequest } from "next/server";
import Groq from "groq-sdk";

const groq = new Groq({ apiKey: process.env.GROQ_API_KEY });

export async function POST(req: NextRequest) {
  const { messages } = await req.json();

  const stream = await groq.chat.completions.create({
    model: "llama-3.3-70b-versatile",
    messages: [
      {
        role: "system",
        content:
          "You are VOID.AI — the built-in intelligence of VOID OS, a cinematic browser-based desktop operating system. You are sharp, concise, and technical. You speak in short paragraphs, never bullet lists unless asked. You embody the aesthetic: monochrome, minimal, precise. Help with code, CS concepts, debugging, system design, and anything the developer asks.",
      },
      ...messages,
    ],
    stream: true,
    max_tokens: 1024,
    temperature: 0.7,
  });

  const encoder = new TextEncoder();

  const readable = new ReadableStream({
    async start(controller) {
      for await (const chunk of stream) {
        const text = chunk.choices[0]?.delta?.content ?? "";
        if (text) controller.enqueue(encoder.encode(text));
      }
      controller.close();
    },
  });

  return new Response(readable, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Transfer-Encoding": "chunked",
    },
  });
}
